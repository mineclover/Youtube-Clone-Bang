import React from 'react';
import styles from './header.module.scss';

type Props = {};

const headerModal = (props: Props) => {
  return <div className={styles.headerModal}>만들기</div>;
};

export default headerModal;
